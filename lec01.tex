\documentclass[12pt]{article}
\usepackage{notes-project}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{listings}

\graphicspath{{lec01/}}
\numberwithin{figure}{section}

\lstset{
  language=Python,
  basicstyle=\ttfamily\footnotesize,
  frame=single,
  framesep=6pt,
  xleftmargin=6pt,
  xrightmargin=6pt,
  breaklines=true,
  breakatwhitespace=true,
  showstringspaces=false,
  columns=fullflexible,
  keepspaces=true,
  keywordstyle=\color{blue!60!black},
  commentstyle=\color{gray},
  stringstyle=\color{green!40!black},
}

\begin{document}

\begin{center}
  {\LARGE\bfseries Lecture 1: Finding Solutions}\\[0.25em]
  {\large Introduction to Data Analysis Techniques}\\[0.15em]
\end{center}

\section{Data Analysis in Science} 

\begin{dfn}[Analytical Method and Analytical Solution]{analytical-method}
  When we use \emph{analytical methods} to solve a problem, we are manipulating the mathematical expressions until unknowns are being isolated into a standard mathematical form. 

  An \emph{analytical solution} is a solution for a problem such that the unknowns are expressed in a closed form. 
\end{dfn}

\begin{eg}[Analytical Solution of a Quadratic Equation]{quadratic-equation}
  Consider the quadratic equation
  \begin{equation}
    ax^2 + bx + c = 0,
  \end{equation}
  where $a$, $b$, and $c$ are constants. The analytic solution for $x$ can be found using the quadratic formula:
  \begin{equation}
    x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}.
  \end{equation} 
\end{eg}

\begin{dfn}[Numerical Method and Numerical Solution]{numerical-method}
  \emph{Numerical methods} are methods to obtain numerical solutions. 

  A \emph{numerical solution} is a solution for a problem such that the unknowns are expressed in an numerical form.
\end{dfn} 

\begin{eg}[Numerical Solution of a Trigonometric Equation]{trig-equation}
  Consider the trigonometric equation
  \begin{equation}
    x = \beta \cos x
  \end{equation} 
  where $\beta$ is a constant. 

  It is very difficult to find an analytical solution for this equation using algebraic manipulations. However, we can use numerical methods to find an approximate solution for specific $\beta$ values. 

  The numerical solutions can be 
  \[
  \beta = 1, \quad x \approx 0.73909, \quad \beta = 0.5, \quad x \approx 0.4508 
  \]
\end{eg} 

\begin{rem}[Advantages and Disadvantages of Numerical Methods]{numerical-methods}
  \begin{enumerate}[nosep] 
    \item For numerical methods, we must have all parameters of the problem known. 
    \item If parameters change, solution must be re-done.
    \item Solutions may be not exact. 
    \item Numerical solutions often considered inferior to analytic solutions, if exist. \textit{ie, we should use analytical methods whenever possible or too difficult.}
  \end{enumerate}

\end{rem} 

\section{Using Taylor Series to Approximate Functions} 
\begin{dfn}[Taylor Series]{taylor-series}
  For a function $f(x)$ that is infinitely differentiable at a point $a$, then: 
  \[f(x) = \sum_{n=0}^{\infty} \frac{f^{(n)}(a)}{n!}(x-a)^n\]
\end{dfn}

\begin{rem}[Taylor Series Approximation]{taylor-series-approx}
  It is impossible to compute the infinite sum in practice. When we find the analytical solution of a problem, we can use the first few terms of the \xref[Taylor Series]{def:taylor-series} to approximate the function.

  The more terms we use, the better the approximation.
\end{rem}

\begin{eg}[Electric Field of a Dipole for $x \gg a$]{dipole-field}
  Consider the electric field on the axis of a dipole with charges $+q$ at $x = a$ and $-q$ at $x = -a$:
  \[E = \frac{q}{4\pi\varepsilon_0} \left[
    \frac{1}{(x-a)^2} - \frac{1}{(x+a)^2}
  \right]\]

  Take the Taylor series expansion of $E$ for $x \gg a$:
  \[E = \frac{q}{4\pi\varepsilon_0 \, x^2} \left[
    4\frac{a}{x} + 8\left(\frac{a}{x}\right)^3 + 12\left(\frac{a}{x}\right)^5 + \cdots
  \right]\]

  Keeping the first two terms gives an approximation for $x \gg a$:
  \[E \approx \frac{q}{4\pi\varepsilon_0 \, x^2} \left[
    4\frac{a}{x} + 8\left(\frac{a}{x}\right)^3
  \right]\]

  Which means, for $x \gg a$, $E \propto x^{-3}$.
\end{eg}

\section{Fixed Point Iteration Method} 

\begin{dfn}[Fixed Point Iteration Method]
  The idea for \emph{fixed point iteration method} is to first find a function $g(x)$ such that root $r$ is a \xref[fixed point]{fundamental-computing::lec07::def:fixed-point} of $g(x)$, i.e., $g(r) = r$
\end{dfn}

\begin{conv}[Procedure of Fixed Point Iteration Method]{fixed-point-iteration}
  The classical procedure of fixed point iteration method is as follows: 
  \begin{enumerate}[nosep]
    \item Find a function $g(x)$ such that root $r$ is a fixed point of $g(x)$, i.e., $g(r) = r$.
    \item Choose an initial guess $x_0$.
    \item Choose a maximum number of iterations $N$ 
    \item Iterate $x_{n+1} = g(x_n)$ for $N$ times 
  \end{enumerate} 
\end{conv}

\color{blue}\begin{rem}[Error Analysis of Fixed Point Iteration Method]{fixed-point-iteration-error}
  By using this method, we usually believe that the root, $r$, is close to the initial guess, $x_0$. Also, it should be within $x_n$ and $x_{n+1}$ forall $n$. 

  Which means, the error of the $n$-th iteration is $|x_n - r| \leq |x_{n+1} - x_n|$. 

  PF TODO 
\end{rem}\color{black}

\begin{conv}[Optimized Fixed Point Iteration]{optimized-fpi}
  We may want to set an error tolerance, $\mathrm{tol}$, and stop the iteration when $|x_{n+1} - x_n| < \mathrm{tol}$. 
    \begin{enumerate}[nosep]
    \item Find a function $g(x)$ such that root $r$ is a fixed point of $g(x)$, i.e., $g(r) = r$.
    \item Choose an initial guess $x_0$.
    \item Choose a maximum number of iterations $N$ 
    \item Set an error tolerance, $\mathrm{tol}$ 
    \item Iterate $x_{n+1} = g(x_n)$ 
    \item Check if $|x_{n+1} - x_n| < \mathrm{tol}$, if yes, stop the iteration and return the value. 
    \item If the maximum number of iterations is reached, stop the iteration and return the value. (For some algorithms, we may want to return an error message instead.)
  \end{enumerate} 

  \lstinputlisting[language=Python]{lec01/find_fixed_point_display.py}
\end{conv}

\begin{eg}[Convergent Case: Fixed Point Iteration on $x - \cos x = 0$]{cos-fixed-point}
  Apply \xref[Optimized Fixed Point Iteration]{conv:optimized-fpi} to $f(x) = x - \cos x = 0$.
  \begin{enumerate}[nosep]
    \item Rewrite $f(x) = 0$ as $x = \cos x$, so $g(x) = \cos x$ and $g(r) = r$.
    \item Initial guess: $x_0 = 1$.
    \item Maximum iterations: $N = 100$.
    \item Error tolerance: $\mathrm{tol} \in \{5 \times 10^{-2},\ 1 \times 10^{-3}\}$ (two runs for comparison).
    \item Iterate $x_{n+1} = \cos x_n$.
    \item Stop when $|x_{n+1} - x_n| < \mathrm{tol}$ and return $x_{n+1}$.
    \item If $n$ reaches $N$, stop and return the last $x_{n+1}$.
  \end{enumerate}

  In Python:
  \lstinputlisting[language=Python]{lec01/fixed_point_demo_1.py}

  Both runs spiral into the fixed point $r \approx 0.739$; a tighter tolerance just adds more steps near $r$. The top row shows the iteration geometrically on the $(x, y)$ plane; the bottom row shows the same iterates plotted against their step number.

  \begin{figure}[h]
    \centering
    \begin{subfigure}[t]{0.45\textwidth}
      \centering
      \includegraphics[width=\linewidth]{fixed_point_demo_1_low_accuracy_cobweb.png}
      \caption{Trace Iteration, $\mathrm{tol} = 5 \times 10^{-2}$.}
      \label{fig:convergent-fpi-a}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.45\textwidth}
      \centering
      \includegraphics[width=\linewidth]{fixed_point_demo_1_high_accuracy_cobweb.png}
      \caption{Trace Iteration, $\mathrm{tol} = 1 \times 10^{-3}$.}
      \label{fig:convergent-fpi-b}
    \end{subfigure}

    \vspace{0.5em}

    \begin{subfigure}[t]{0.45\textwidth}
      \centering
      \includegraphics[width=\linewidth]{fixed_point_demo_1_low_accuracy_sequence.png}
      \caption{$x_n$ vs. $n$, $\mathrm{tol} = 5 \times 10^{-2}$.}
      \label{fig:convergent-fpi-c}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.45\textwidth}
      \centering
      \includegraphics[width=\linewidth]{fixed_point_demo_1_high_accuracy_sequence.png}
      \caption{$x_n$ vs. $n$, $\mathrm{tol} = 1 \times 10^{-3}$.}
      \label{fig:convergent-fpi-d}
    \end{subfigure}
    \caption{Convergent case: fixed point iteration on $x - \cos x = 0$ from $x_0 = 1$.}
    \label{fig:convergent-fpi}
  \end{figure}

  \begin{itemize}[nosep]
    \item Figure~\ref{fig:convergent-fpi}\subref{fig:convergent-fpi-a}: x-axis is $x$, y-axis is $y$; shows $y = g(x) = \cos x$ (blue), the diagonal $y = x$ (dashed), and an orange path that traces the iteration 
    \item Figure~\ref{fig:convergent-fpi}\subref{fig:convergent-fpi-b}: same axes as (a); tighter $\mathrm{tol} = 1 \times 10^{-3}$ makes the orange path pack more segments around the fixed point $r$.
    \item Figure~\ref{fig:convergent-fpi}\subref{fig:convergent-fpi-c}: x-axis is the iteration index $n$, y-axis is the iterate $x_n$; shows the sequence of iterates as damped oscillation for $\mathrm{tol} = 5 \times 10^{-2}$, with the dashed line marking the final $x_n$.
    \item Figure~\ref{fig:convergent-fpi}\subref{fig:convergent-fpi-d}: same axes as (c); tighter run $\mathrm{tol} = 1 \times 10^{-3}$ settles visibly onto $r \approx 0.739$.
  \end{itemize}
\end{eg}

\begin{rem}[Convergence not Guaranteed]{fixed-point-iteration-convergence}
  The convergence of fixed point iteration is not guaranteed.
\end{rem}

\begin{eg}[Divergent Case: $x - 1.5 \cos x = 0$]{divergent-fpi}
  Same procedure as \xref{eg:cos-fixed-point}, but with $g(x) = 1.5 \cos x$ and $x_0 = 1$. It diverges and oscillates between $\approx 0.123$ and $\approx 1.489$; both runs hit $N = 100$ without ever satisfying the stop condition.

  \begin{figure}[h]
    \centering
    \begin{subfigure}[t]{0.45\textwidth}
      \centering
      \includegraphics[width=\linewidth]{fixed_point_demo_2_low_accuracy_cobweb.png}
      \caption{Trace Iteration, $\mathrm{tol} = 5 \times 10^{-2}$.}
      \label{fig:divergent-fpi-a}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.45\textwidth}
      \centering
      \includegraphics[width=\linewidth]{fixed_point_demo_2_high_accuracy_cobweb.png}
      \caption{Trace Iteration, $\mathrm{tol} = 1 \times 10^{-3}$.}
      \label{fig:divergent-fpi-b}
    \end{subfigure}

    \vspace{0.5em}

    \begin{subfigure}[t]{0.45\textwidth}
      \centering
      \includegraphics[width=\linewidth]{fixed_point_demo_2_low_accuracy_sequence.png}
      \caption{$x_n$ vs. $n$, $\mathrm{tol} = 5 \times 10^{-2}$.}
      \label{fig:divergent-fpi-c}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.45\textwidth}
      \centering
      \includegraphics[width=\linewidth]{fixed_point_demo_2_high_accuracy_sequence.png}
      \caption{$x_n$ vs. $n$, $\mathrm{tol} = 1 \times 10^{-3}$.}
      \label{fig:divergent-fpi-d}
    \end{subfigure}
    \caption{Divergent case: fixed point iteration on $x - 1.5 \cos x = 0$ from $x_0 = 1$.}
    \label{fig:divergent-fpi}
  \end{figure}
\end{eg} 

\begin{rem}[Advantage and Disadvantage of Fixed Point Iteration Method]{fpi-evaluation}
  \begin{enumerate}[nosep]
    \item Advantage: It is easy to implement. 
    \item Disadvantage: The convergence is not guaranteed. 
  \end{enumerate}
  Usually used for \emph{good initial guess} and \emph{good $g(x)$ function}. 
\end{rem} 

\section{Bisector Method}

\begin{dfn}[Bisector Method]{bisector-method}
  The idea of \emph{bisector method} is when we know that a root $r$ is in the interval $[a, b]$, then we can find the midpoint of the range and evaluate which part of the interval contains the root. We can then repeat this process until we find a sufficiently accurate approximation of the root. 
\end{dfn} 

\begin{conv}[Procedure of Bisector Method]{bisector-method-procedure}
  The procedure of bisector method is as follows: 
  \begin{enumerate}[nosep]
    \item Find an interval $[a, b]$ such that $\exists r \in [a, b] \text{ st } f(r) = 0$ and $f(a)f(b) < 0$. 
    \item Set the maximum number of iterations, $N$. 
    \item Find the midpoint of the interval, $c = \frac{a+b}{2}$.
    \item Evaluate $f(c)$.
    \item If $f(a)$ and $f(c)$ have opposite signs, then set $b = c$; otherwise, set $a = c$. 
    \item If the maximum number of iterations is reached, stop the iteration and return the value. Otherwise goto step 3. 
  \end{enumerate} 
\end{conv}

\begin{proof}[Bisector Method Convergence]

\end{proof}

\begin{rem}[Error Analysis of Bisector Method]{bisector-method-error}
  By using this method, we usually believe that the root, $r$, is in the interval $[a, b]$. 

  Which means, we can have a one to one map between the number of iterations and the error tolerance, $tol$, 
  \[tol = |a - b| \times 2^{-n}\]
\end{rem} 

\end{document}
